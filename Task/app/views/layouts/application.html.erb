<!DOCTYPE html>
<html>
    <head>
        <title>Task</title>
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>
    </head>
    <body>

        <header>
            <h1>Task List App</h1>
        </header>

        <div id="primary">
            <% if flash[:message] %>
            <p class="flash-message"><%= flash[:message] %></p>
            <% end %>
            <%# yield  -- jarp: this duplicates your app. Use the yield below %>
        </div>

        <div id="sidebar">
            <%= render 'layouts/sidebar' %>
        </div>

        <main>
            <%= yield %>
        </main>

        <footer>
            <p>
                This app was built by Gaurav Sirdeshpande for MGTI 40660 Building Web Applications.
            </p>
            <% if session[:user_id] %>
              <%= link_to 'Logout', logout_path %>
            <% else %>
              <%= link_to 'Login', login_path %>
            <% end %>
        </footer>

    </body>
</html>
